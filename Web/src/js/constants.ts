export default Object.seal({
    URLs: {
        BACKEND_URL: "http://localhost",
        token: "/api/token",
        login: "/api/login",
        logout: "/api/logout",
        getUser: "/api/data",
        getSicherheitsfragen: "/api/sicherheitsfragen",
    }
    
});